{
  "name": "app",
  "version": "1.0.50",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "test": "bun test",
    "lint": "eslint . --ext .ts",
    "stage": "BUN_ENV=test bun run dist/server.js",
    "prod": "BUN_ENV=prod bun run dist/server.js",
    "prisma:generate": "bun run prisma generate",
    "prisma:migrate": "bun run prisma migrate deploy",
    "seed": "bun run prisma/database-seed.ts",
    "build": "bun run build:bundle && bun run build:copy",
    "build:bundle": "bun build src/server.ts --outfile dist/server.js --minify",
    "build:copy": "cp .env dist/.env && cp -R prisma dist/prisma"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.3.3",
    "@elysiajs/opentelemetry": "^1.3.1",
    "@elysiajs/swagger": "^1.3.1",
    "@opentelemetry/exporter-trace-otlp-proto": "^0.203.0",
    "@opentelemetry/sdk-trace-node": "^2.0.1",
    "@prisma/client": "^6.12.0",
    "dotenv": "^17.2.1",
    "elysia": "latest",
    "pg": "^8.15.6",
    "prisma": "^6.12.0",
    "zod": "^4.0.10"
  },
  "devDependencies": {
    "@opentelemetry/instrumentation-pg": "^0.55.0",
    "@typescript-eslint/eslint-plugin": "^8.38.0",
    "@typescript-eslint/parser": "^8.38.0",
    "bun-types": "latest",
    "eslint": "^9.32.0"
  },
  "module": "src/server.js"
}
